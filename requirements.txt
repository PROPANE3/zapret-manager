# Зависимостей нет — только стандартная библиотека Python 3.8+.
# Это сделано специально для минимального потребления ресурсов (~15-25 МБ RAM).
# tkinter входит в стандартный установщик Python для Windows.
# Если tkinter отсутствует: переустановите Python с галочкой "tcl/tk and IDLE".
